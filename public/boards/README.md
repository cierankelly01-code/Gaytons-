# Board photos

Drop your board photos here. They are referenced from `lib/menu.ts`:

- `small.jpg`
- `medium.jpg`
- `large.jpg`

If a file is missing, the site shows a warm colour block with the board name
instead — nothing looks broken. Recommended: landscape JPGs, ~1200×800px,
optimised (under ~300KB each).
