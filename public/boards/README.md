# Board & theme photos

Drop your photos here. They're referenced from `lib/menu.ts`:

**Buy-now boards** (`fixedBoards`):
- `small.jpg`
- `medium.jpg`
- `large.jpg`

**Build-a-board styles** (`configThemes`):
- `savoury.jpg`
- `salmon.jpg`
- `cheese.jpg`
- `sandwich.jpg`

If a file is missing, the app shows a warm colour block with the name instead —
nothing looks broken, so you can add photos whenever you're ready. Recommended:
landscape JPGs, ~1200×800px, optimised (under ~300KB each).
